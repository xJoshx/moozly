import React, { PureComponent } from 'react';
import { View } from 'react-native';
import PropTypes from 'prop-types';

class Profile extends PureComponent {
  render() {
    return <View>Holi</View>;
  }
}

Profile.propTypes = {};

export default Profile;
